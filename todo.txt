x (C) [pkg] вынести client/tcpclient в pkg
x (С) [aoimdb] выпиздить grpc
(A) [aoimdb] написать сервисер для всех сервисов которые запускаются в коре
    запуск листенера для тсп коннектов, ui, grpc обернуть в структуру сервиса,
    у которого будет метод типа listenandServe а не просто ожидание ошибки в потоке ожиданния ошибки
x (B) [aoimdb] перейти с msg_protocol на обычный json, нахуй этот цирк
x     [aoimdb-cli]получать в aoimdb-cli json и "рисовать" как есть
(С) подумать над переделкой api/commands
    перейти на формат запросов из protocols
(В) [aoimdb] вынести все кроме datatypes из internal/aoimdb в cmd/aoimdb
(B) [examples] сделать из examples/client подобие интеграционного теста
    запускаем на фоне датабейз,коннектимся к нему же
(B) [aoimdb] получать креды к админ юзеру из флагов
(B) [aoimdb] разделить пользователей по ролям

----

feature bets
1) http api with bearer auth
2) интеграционные тесты через гитхаб экшенс
3) индексы к типу данных таблица
4) веб интерфейс
5) hset