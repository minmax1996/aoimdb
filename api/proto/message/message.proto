syntax = "proto3";
option go_package = "api/proto/message";

service Messanger {
  rpc SendMessage(DefaultMessage) returns (DefaultMessage);
}

message DefaultMessage {
  string query = 1;
  string param = 2;
}
